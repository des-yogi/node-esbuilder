# Чек-лист тестирования node-esbuilder

> Тестировать на реальном или тестовом проекте.
> Отмечай ✅ / ❌ по мере прохождения.

---

## 0. Установка и запуск

- [x] `npm i` — зависимости ставятся без ошибок
- [x] `npm run dev` — dev-сервер стартует, браузер открывается
- [x] `npm run build` — production-сборка завершается без ошибок
- [x] `build/` содержит css/, js/, img/, fonts/, *.html после сборки

---

## 1. Очистка (clean.mjs)

- [x] `npm run build` — папка `build/` полностью очищается перед сборкой
- [x] Старые файлы от предыдущей сборки НЕ остаются

---

## 2. Создание блока (createBlock.mjs)

- [x] `npm run create-block -- my-block` — создаёт `src/blocks/my-block/`
- [x] Создаются файлы: `my-block.scss`, `my-block.html`, папки `img/`, `bg-img/`
- [x] `my-block` автоматически добавлен в `projectConfig.json` → `blocks`
- [x] `npm run create-block -- my-block js` — дополнительно создаёт `my-block.js`
- [x] Повторный вызов НЕ перезаписывает существующие файлы
- [x] Вызов без имени блока — выдаёт ошибку в консоль
- [x] Содержимое `.scss` — с `@use '../../scss/variables' as *;` и шаблоном БЭМ
- [x] Содержимое `.html` — с DEV-комментарием и `<div class="my-block">`

---

## 3. Генерация style.scss (generateStyle.mjs)

- [x] `npm run gen:style` — генерирует `src/scss/style.scss`
- [x] В style.scss — комментарий «не редактируйте вручную»
- [x] addCssBefore идёт ПЕРЕД блоками с `as *`
- [x] Блоки подключаются через `@use` в порядке ключей из `projectConfig.blocks`
- [x] addCssAfter идёт ПОСЛЕ блоков с `as *`
- [x] Пути преобразуются корректно (src/scss/variables → ./variables)
- [x] Пути к блокам преобразуются (src/blocks/page/page.scss → ../blocks/page/page)

---

## 4. Компиляция стилей (styles.mjs)

### 4.1 Основной бандл

- [x] `build/css/style.min.css` создаётся
- [x] Dev-режим: стили в expanded-формате + `.map` файл рядом
- [x] Production-режим: стили сжаты (compressed), без .map

### 4.2 PostCSS-плагины

- [x] Autoprefixer работает (проверить: `display: flex` → `-webkit-` если нужно по browserslist)
- [x] `browserslist` из `package.json` подхватывается
- [x] Сортировка media-queries работает (мобильные-first)
- [x] `postcss-inline-svg` работает (SVG в CSS background)
- [x] `customPostcss.js` — если добавить плагин, он применяется

### 4.3 singleCompiled

- [x] Добавить в конфиг: `"singleCompiled": ["src/scss/admin.scss"]`
- [x] Создать `src/scss/admin.scss` с каким-то содержимым
- [x] После сборки — `build/css/admin.min.css` создаётся отдельно
- [x] `admin.min.css` НЕ попадает в `style.min.css`
- [x] PostCSS-плагины применяются и к singleCompiled

### 4.4 copiedCss

- [x] Файлы из `copiedCss` копируются в `build/css/` без изменений
- [x] Проверить: `build/css/bootstrap.min.css` — идентичен исходному

### 4.5 Ошибки

- [x] Синтаксическая ошибка в SCSS — сборка не крашится, ошибка в консоли

---

## 5. Сборка JS (scripts.mjs)

### 5.1 Основной бандл

- [x] `build/js/script.min.js` создаётся
- [x] Dev-режим: без минификации + `.map` файл
- [x] Production-режим: минифицированный, без .map

### 5.2 Порядок файлов в бандле

- [x] `src/js/index.js` выполняется вторым, а не первым (no-js → js, vh fix)
- [x] addJsBefore → src/js/index.js → блоковые JS → addJsAfter — порядок соблюдается
- [x] Если `src/js/index.js` указан в `addJsAfter` — НЕ дублируется

### 5.3 Блоковые JS

- [x] JS-файлы блоков подключаются, если существуют
- [x] Отсутствие JS-файла у блока НЕ вызывает ошибку
- [x] JS блока выполняется корректно в итоговом бандле

### 5.4 copiedJs

- [x] Файлы из `copiedJs` копируются в `build/js/` без обработки
- [x] Проверить: `build/js/bootstrap.bundle.min.js` — идентичен исходному

### 5.5 npm-пакеты

- [x] `import` из `node_modules` в JS блока — esbuild подтягивает
- [x] CJS-пакет через `import` — работает (esbuild конвертирует)
- [x] ESM-пакет через `import` — работает

### 5.6 Формат бандла

- [x] Формат IIFE (не загрязняет глобальный скоуп)
- [x] `target: es2019` — проверить: стрелочные функции сохраняются, но async generators нет

### 5.7 Функционал index.js

- [x] `no-js` → `js` на `<html>` — работает
- [x] `--vh` CSS-переменная устанавливается
- [x] Lazy-load фонов (`data-bg` / `data-bg-webp`) — работает при скролле
- [x] Theme switcher — кнопка `#toggle-theme` переключает тему
- [x] Theme switcher — сохраняет выбор в localStorage
- [x] Theme switcher — синхронизация между вкладками

### 5.8 Ошибки

- [x] Синтаксическая ошибка в JS — сборка не крашится, ошибка в консоли

---

## 6. Сборка HTML (html.mjs)

### 6.1 Базовое

- [x] Все `src/*.html` → `build/*.html`
- [x] ��есколько HTML-файлов в src/ — все обрабатываются

### 6.2 @@include

- [x] Простой `@@include('blocks/header/header.html')` — подключается
- [x] `@@include('_include/page_head.html')` — подключается
- [x] Вложенные @@include (include внутри include) — работают
- [x] Пути с поддиректориями — корректно резолвятся

### 6.3 Переменные

- [x] `@@include('...', { "title": "Привет" })` + `@@title` в шаблоне — подставляется
- [x] Вложенные переменные: `@@user.name` → значение из `{ "user": { "name": "Иван" } }`
- [x] `@@img.jpg` (не переменная, а часть имени файла) — НЕ ломает парсинг
- [x] Отсутствующая переменная — заменяется на пустую строку (без ошибки)

### 6.4 DEV-комментарии

- [x] `<!--DEV ... -->` — удаляются из итогового HTML
- [x] `@@include` внутри DEV-комментария — НЕ обрабатывается

### 6.5 Ошибки

- [x] Циклический include (A→B→A) — ошибка с понятным сообщением, без зависания
- [x] Несуществующий файл в @@include — ошибка с путём файла

---

## 7. Копирование ассетов (assets.mjs)

### 7.1 Шрифты

- [x] `src/fonts/**` → `build/fonts/**` — с сохранением структуры
- [x] Все форматы (ttf, woff, woff2, eot, svg) — копируются

### 7.2 Глобальные картинки

- [x] `src/img/**` → `build/img/**` — с сохранением структуры подпапок
- [x] Фильтрация по `allowedImageExtensions` работает
- [x] Файлы с недопустимым расширением — пропускаются с логом

### 7.3 Картинки блоков

- [x] `src/blocks/*/img/*` → `build/img/` — плоская схема (без вложенности)
- [x] Фильтрация по расширениям работает

### 7.4 Глобальное видео

- [x] `src/video/**` → `build/video/**` — с сохранением структуры
- [x] Фильтрация по `allowedVideoExtensions` работает

### 7.5 Видео блоков

- [x] `src/blocks/*/video/*` → `build/video/` — плоская схема
- [ ] Уникализация имён при коллизиях (name.mp4 → name-1.mp4)

### 7.6 Инкрементальность (isNewer)

- [x] Повторный `npm run build` (без clean) — неизменённые файлы НЕ копируются
- [x] Изменённый файл — копируется заново
- [x] Новый файл — копируется

### 7.7 Отсутствующие папки

- [ ] Нет `src/fonts/` — предупреждение в лог, сборка не падает (сборка зависает)
- [x] Нет `src/video/` — предупреждение в лог, сборка не падает
- [x] Нет `src/blocks/some-block/img/` — пропускается без ошибки

---

## 8. SVG-спрайт (sprite-svg.mjs)

### 8.1 Базовое

- [x] Создать `src/blocks/sprite-svg/svg/` с несколькими SVG
- [x] После сборки — `build/img/sprite-svg.svg` существует
- [x] Содержит `<symbol>` для каждого SVG-файла
- [x] `id` символа = имя файла без `.svg`

### 8.2 Атрибуты

- [x] `viewBox` из исходного SVG → переносится в `<symbol>`
- [x] `width`, `height`, `fill` — переносятся если есть
- [x] `xmlns:xlink` — переносится если есть

### 8.3 Отсутствие

- [x] Нет папки `src/blocks/sprite-svg/svg/` — предупреждение, сборка не падает
- [x] Папка есть, но пустая — предупреждение, сборка не падает

---

## 9. Оптимизация картинок (img-opt.mjs)

### 9.1 Базовое

- [x] `npm run img:opt -- src/img build/img-test` — работает
- [x] JPG → оптимизированный JPG (mozjpeg) + WebP + AVIF
- [x] PNG → оптимизированный PNG + WebP + AVIF
- [x] SVG → копируется без изменений
- [x] ICO → копируется без изменений
- [x] GIF → копируется без изменений

### 9.2 Вложенные папки

- [x] Структура подпапок сохраняется на выходе
- [x] `design/a/b/photo.jpg` → `output/a/b/photo.jpg` + `.webp` + `.avif`

### 9.3 Защита

- [ ] Входная = выходная папка → ошибка, скрипт не запускается
- [ ] Без аргументов → подсказка по использованию
- [ ] Неизвестный формат (`.bmp`) — пропускается с предупреждением

### 9.4 Внешние пути

- [x] `npm run img:opt -- design/originals design/optimized` — работает
- [x] Абсолютные пути — работают

### 9.5 Статистика

- [x] По завершении — выводит итог (оптимизировано, WebP, AVIF, скопировано, пропущено)

---

## 10. Dev-сервер (dev-server.mjs)

### 10.1 Запуск

- [x] `npm run dev` — полная сборка → browser-sync → вотчеры
- [x] Браузер открывается автоматически
- [x] Страница отображается корректно

### 10.2 Вотчер: SCSS

- [x] Изменение `.scss` файла блока → стили перекомпилируются → браузер обновляется
- [x] Изменение `src/scss/variables.scss` → стили перекомпилируются
- [x] Изменение `src/scss/style.scss` — НЕ вызывает перегенерацию style.scss (иначе цикл)

### 10.3 Вотчер: JS

- [x] Изменение `.js` файла блока → скрипты пересобираются → браузер обновляется

### 10.4 Вотчер: HTML

- [x] Изменение `src/index.html` → HTML пересобирается → браузер обновляется
- [x] Изменение `src/_include/*.html` → HTML пересобирается
- [x] Изменение `src/blocks/*/block.html` → HTML пересобирается

### 10.5 Вотчер: ассеты

- [x] Добавление картинки в `src/img/` → копируется в `build/img/`
- [x] Добавление шрифта в `src/fonts/` → копируется в `build/fonts/`

### 10.6 Вотчер: projectConfig.json

- [x] Изменение `projectConfig.json` → полная пересборка (без clean)
- [x] Добавление нового блока в `blocks` → style.scss перегенерируется

### 10.7 Защита от параллельных запусков

- [x] Быстрое двойное сохранение — не крашит сервер (isRebuilding)

---

## 11. projectConfig.json

### 11.1 Валидность

- [x] Все поля присутствуют и читаются без ошибок
- [x] Пустые массивы (`addJsBefore: []`) — не вызывают ошибок
- [x] Отсутствующие опциональные поля — подставляются дефолты

### 11.2 allowedImageExtensions

- [x] Файлы с расширениями из списка — копируются
- [x] Файлы с расширениями НЕ из списка — пропускаются

### 11.3 allowedVideoExtensions

- [x] Аналогично для видео

---

## 12. Логирование (logger.mjs)

- [x] Цветной вывод в консоль — теги раскрашены

- [x] `[build]`, `[styles]`, `[scripts]`, `[assets]`, `[html]`,
  
      `[sprite-svg]`, `[dev-server]`, `[createBlock]`, `[generateStyle]` — свой цвет

- [x] `[img-opt]` — использует defaultTagStyle (или свой, если добавлен)

- [x] logSuccess — зелёный

- [x] logInfo — серый

- [x] logWarn — жёлтый

- [x] logError — красный

---

## 13. Линтинг

- [x] `npm run lint:css` — запускается, проверяет SCSS
- [x] `npm run lint:js` — запускается, проверяет JS
- [x] При наличии ошибок — выводятся в консоль

---

## 14. Кроссплатформенность

- [x] Windows — `npm run dev` работает
- [x] Windows — `npm run build` работает
- [x] Windows — `npm run img:opt` работает
- [ ] macOS/Linux — всё из вышеперечисленного работает -- Проверь невозможно
- [x] Пути с обратными слэшами (Windows) — корректно нормализуются

---

## 15. Edge-кейсы

- [x] Пустой `blocks: {}` — сборка проходит (пустой style.min.css, пустой script.min.js)
- [ ] Блок указан в конфиге, но папки нет → предупреждение, сборка не падает -- На данный момент сборка падает, но возможно это и правильно, иначе можно и не заметить, например, что блок удален...
- [ ] Блок без `.scss` файла → предупреждение -- Сейчас Сервер не запускается с ошибкой...
- [x] Блок без `.js` файла → тишина (это нормально)
- [x] Очень длинное имя блока → не ломает пути
- [x] Кириллица в имени файла → проверить (может понадобиться)
- [x] Пробелы в пути проекта → проверить