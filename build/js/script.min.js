(() => {
  var __create = Object.create;
  var __defProp = Object.defineProperty;
  var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
  var __getOwnPropNames = Object.getOwnPropertyNames;
  var __getProtoOf = Object.getPrototypeOf;
  var __hasOwnProp = Object.prototype.hasOwnProperty;
  var __commonJS = (cb, mod) => function __require() {
    return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
  };
  var __copyProps = (to, from, except, desc) => {
    if (from && typeof from === "object" || typeof from === "function") {
      for (let key of __getOwnPropNames(from))
        if (!__hasOwnProp.call(to, key) && key !== except)
          __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
    }
    return to;
  };
  var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
    // If the importer is in node compatibility mode or this is not an ESM
    // file that has been converted to a CommonJS file using a Babel-
    // compatible transform (i.e. "__esModule" has not been set), then set
    // "default" to the CommonJS "module.exports" for node compatibility.
    isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
    mod
  ));

  // node_modules/autosize/dist/autosize.min.js
  var require_autosize_min = __commonJS({
    "node_modules/autosize/dist/autosize.min.js"(exports, module) {
      !(function(e, t) {
        "object" == typeof exports && "undefined" != typeof module ? module.exports = t() : "function" == typeof define && define.amd ? define(t) : (e || self).autosize = t();
      })(exports, function() {
        var e = /* @__PURE__ */ new Map();
        function t(t2) {
          var o2 = e.get(t2);
          o2 && o2.destroy();
        }
        function o(t2) {
          var o2 = e.get(t2);
          o2 && o2.update();
        }
        var r = null;
        return "undefined" == typeof window ? ((r = function(e2) {
          return e2;
        }).destroy = function(e2) {
          return e2;
        }, r.update = function(e2) {
          return e2;
        }) : ((r = function(t2, o2) {
          return t2 && Array.prototype.forEach.call(t2.length ? t2 : [t2], function(t3) {
            return (function(t4) {
              if (t4 && t4.nodeName && "TEXTAREA" === t4.nodeName && !e.has(t4)) {
                var o3, r2 = null, n = window.getComputedStyle(t4), i = (o3 = t4.value, function() {
                  s({ testForHeightReduction: "" === o3 || !t4.value.startsWith(o3), restoreTextAlign: null }), o3 = t4.value;
                }), l = function(o4) {
                  t4.removeEventListener("autosize:destroy", l), t4.removeEventListener("autosize:update", a), t4.removeEventListener("input", i), window.removeEventListener("resize", a), Object.keys(o4).forEach(function(e2) {
                    return t4.style[e2] = o4[e2];
                  }), e.delete(t4);
                }.bind(t4, { height: t4.style.height, resize: t4.style.resize, textAlign: t4.style.textAlign, overflowY: t4.style.overflowY, overflowX: t4.style.overflowX, wordWrap: t4.style.wordWrap });
                t4.addEventListener("autosize:destroy", l), t4.addEventListener("autosize:update", a), t4.addEventListener("input", i), window.addEventListener("resize", a), t4.style.overflowX = "hidden", t4.style.wordWrap = "break-word", e.set(t4, { destroy: l, update: a }), a();
              }
              function s(e2) {
                var o4, i2, l2 = e2.restoreTextAlign, a2 = void 0 === l2 ? null : l2, d = e2.testForHeightReduction, u = void 0 === d || d, f = n.overflowY;
                if (0 !== t4.scrollHeight && ("vertical" === n.resize ? t4.style.resize = "none" : "both" === n.resize && (t4.style.resize = "horizontal"), u && (o4 = (function(e3) {
                  for (var t5 = []; e3 && e3.parentNode && e3.parentNode instanceof Element; ) e3.parentNode.scrollTop && t5.push([e3.parentNode, e3.parentNode.scrollTop]), e3 = e3.parentNode;
                  return function() {
                    return t5.forEach(function(e4) {
                      var t6 = e4[0], o5 = e4[1];
                      t6.style.scrollBehavior = "auto", t6.scrollTop = o5, t6.style.scrollBehavior = null;
                    });
                  };
                })(t4), t4.style.height = ""), i2 = "content-box" === n.boxSizing ? t4.scrollHeight - (parseFloat(n.paddingTop) + parseFloat(n.paddingBottom)) : t4.scrollHeight + parseFloat(n.borderTopWidth) + parseFloat(n.borderBottomWidth), "none" !== n.maxHeight && i2 > parseFloat(n.maxHeight) ? ("hidden" === n.overflowY && (t4.style.overflow = "scroll"), i2 = parseFloat(n.maxHeight)) : "hidden" !== n.overflowY && (t4.style.overflow = "hidden"), t4.style.height = i2 + "px", a2 && (t4.style.textAlign = a2), o4 && o4(), r2 !== i2 && (t4.dispatchEvent(new Event("autosize:resized", { bubbles: true })), r2 = i2), f !== n.overflow && !a2)) {
                  var c = n.textAlign;
                  "hidden" === n.overflow && (t4.style.textAlign = "start" === c ? "end" : "start"), s({ restoreTextAlign: c, testForHeightReduction: true });
                }
              }
              function a() {
                s({ testForHeightReduction: true, restoreTextAlign: null });
              }
            })(t3);
          }), t2;
        }).destroy = function(e2) {
          return e2 && Array.prototype.forEach.call(e2.length ? e2 : [e2], t), e2;
        }, r.update = function(e2) {
          return e2 && Array.prototype.forEach.call(e2.length ? e2 : [e2], o), e2;
        }), r;
      });
    }
  });

  // src/js/index.js
  function applyJsClass() {
    document.documentElement.className = document.documentElement.className.replace("no-js", "js");
  }
  function setupVhFix() {
    var setVh = function() {
      var vh = window.innerHeight * 0.01;
      document.documentElement.style.setProperty("--vh", vh + "px");
    };
    setVh();
    window.addEventListener("resize", setVh);
  }
  function canUseWebp() {
    var elem = document.createElement("canvas");
    return !!(elem.getContext && elem.getContext("2d")) && elem.toDataURL("image/webp").indexOf("data:image/webp") === 0;
  }
  function loadBackgroundImage(element, fastScroll) {
    var isWebpSupported = canUseWebp();
    var bgImage = isWebpSupported ? element.getAttribute("data-bg-webp") : element.getAttribute("data-bg");
    if (!bgImage) return;
    element.style.backgroundImage = "url(" + bgImage + ")";
    element.removeAttribute("data-bg");
    element.removeAttribute("data-bg-webp");
    var speed = parseFloat(element.getAttribute("data-bg-speed")) || 1;
    var finalSpeed = fastScroll ? speed * 0.5 : speed;
    requestAnimationFrame(function() {
      element.style.transition = "opacity " + finalSpeed + "s ease-out";
      element.style.opacity = 1;
    });
  }
  function observeLazyLoad() {
    var lazyElements = document.querySelectorAll("[data-bg], [data-bg-webp]");
    lazyElements.forEach(function(el) {
      el.style.opacity = "0";
      el.style.willChange = "opacity";
    });
    var lastTime = performance.now();
    var lastScrollY = window.scrollY;
    if ("IntersectionObserver" in window) {
      var observer = new IntersectionObserver(function(entries, obs) {
        var now = performance.now();
        var deltaY = Math.abs(window.scrollY - lastScrollY);
        var deltaT = now - lastTime;
        var speed = deltaY / (deltaT || 1);
        var fastScroll = speed > 1;
        entries.forEach(function(entry) {
          if (entry.isIntersecting) {
            loadBackgroundImage(entry.target, fastScroll);
            obs.unobserve(entry.target);
          }
        });
        lastTime = now;
        lastScrollY = window.scrollY;
      }, { rootMargin: "0px", threshold: 0.1 });
      lazyElements.forEach(function(el) {
        observer.observe(el);
      });
    } else {
      lazyElements.forEach(function(el) {
        loadBackgroundImage(el, false);
      });
    }
  }
  function initThemeSwitcher() {
    var STORAGE_KEY = "theme";
    var prefersDark = window.matchMedia("(prefers-color-scheme: dark)");
    var transitionTimeout;
    var lastToggleTime = 0;
    function setThemeAttribute(theme, smooth) {
      if (smooth) {
        clearTimeout(transitionTimeout);
        document.documentElement.classList.add("theme-transition");
        transitionTimeout = setTimeout(function() {
          document.documentElement.classList.remove("theme-transition");
        }, 350);
      }
      document.documentElement.setAttribute("data-theme", theme);
    }
    function applyTheme(theme, smooth) {
      setThemeAttribute(theme, smooth);
      localStorage.setItem(STORAGE_KEY, theme);
    }
    function applySystemTheme(smooth) {
      var systemTheme = prefersDark.matches ? "dark" : "light";
      setThemeAttribute(systemTheme, smooth);
      localStorage.removeItem(STORAGE_KEY);
    }
    function toggleTheme() {
      var now = Date.now();
      if (now - lastToggleTime < 500) return;
      lastToggleTime = now;
      var current = document.documentElement.getAttribute("data-theme") || "light";
      var next = current === "dark" ? "light" : "dark";
      applyTheme(next, true);
    }
    window.addEventListener("storage", function(e) {
      if (e.key === STORAGE_KEY) {
        lastToggleTime = Date.now();
        if (e.newValue === null) {
          setThemeAttribute(prefersDark.matches ? "dark" : "light", true);
        } else if (e.newValue === "dark" || e.newValue === "light") {
          setThemeAttribute(e.newValue, true);
        }
      }
    });
    function initDOM() {
      var saved = localStorage.getItem(STORAGE_KEY);
      if (saved === "dark" || saved === "light") {
        setThemeAttribute(saved, false);
      } else {
        setThemeAttribute(prefersDark.matches ? "dark" : "light", false);
      }
      requestAnimationFrame(function() {
        document.documentElement.classList.remove("no-transition");
      });
      var toggleBtn = document.getElementById("toggle-theme");
      if (toggleBtn) {
        toggleBtn.removeEventListener("click", toggleTheme);
        toggleBtn.addEventListener("click", toggleTheme);
      }
      prefersDark.addEventListener("change", function() {
        if (!localStorage.getItem(STORAGE_KEY)) {
          applySystemTheme(true);
        }
      });
    }
    if (document.readyState === "loading") {
      document.addEventListener("DOMContentLoaded", initDOM);
    } else {
      initDOM();
    }
  }
  function initGlobal() {
    applyJsClass();
    setupVhFix();
    if (document.readyState === "loading") {
      document.addEventListener("DOMContentLoaded", observeLazyLoad);
    } else {
      observeLazyLoad();
    }
    initThemeSwitcher();
  }
  initGlobal();

  // <stdin>
  var import_autosize_min = __toESM(require_autosize_min());
})();
//# sourceMappingURL=script.min.js.map
