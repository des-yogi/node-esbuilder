// Массив пользовательских PostCSS-плагинов. Для расширения — добавить свои require() или import.
export default [
  // require('postcss-pxtorem')({ rootValue: 16, propList: ['*'] }),
  // require('postcss-color-function')()
];
